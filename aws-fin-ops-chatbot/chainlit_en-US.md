# AWS FinOps Copilot (en-US)

Welcome! This localized welcome screen mirrors the default `chainlit.md` content so the app no longer logs missing-translation warnings when `CHAINLIT_LANGUAGE=en-US` is set. Customize this file if you want US-specific messaging, otherwise it can stay aligned with the default markdown.

- Need to tweak the UI copy? Update this file and restart Chainlit.
- Prefer to hide the welcome screen entirely? Leave both markdown files empty.

Happy building!
